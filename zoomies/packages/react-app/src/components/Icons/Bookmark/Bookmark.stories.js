import React from 'react';

import Bookmark from './Bookmark';

const AddStory = {
  title: 'Icons/Individual_Icons/Bookmark',
  component: Bookmark,
  argTypes: {}
};

const Template = () => <Bookmark />;

export const Default = Template.bind({});

export default AddStory;
